#!/bin/bash

# Install NVM into workdir so it persists updates to WireStarter
curl https://raw.githubusercontent.com/creationix/nvm/master/install.sh | HOME=/opt bash
echo "'exec bash' to update your environment!"
